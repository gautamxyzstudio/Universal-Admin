import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: "#FF7312",
    },
    secondary: {
      main: "#CCD4F2",
    },
   
  },
});

export default theme;
