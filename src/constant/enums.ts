export enum IFieldTypes {
  SIMPLE = 'O0',
  EMAIL = 'o1',
  PASSWORD = 'o2',
  MOBILE = 'o3',
  STATUS = 'o4',
}

//enums
export enum IDocumentStatus {
  PENDING = 's0',
  APPROVED = 's1',
  DENIED = 's2',
  UPDATE = 's3',
}

export enum IClientStatus {
  PENDING = 's0',
  ACTIVE = 's1',
  INACTIVE = 's2',
}
